<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="50sp"
    android:paddingTop="@dimen/halfGrid"
    android:paddingBottom="@dimen/halfGrid"
    android:paddingLeft="@dimen/fullGrid"
    android:paddingRight="@dimen/fullGrid"
    android:stretchColumns="*">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="left">
    <TextView
        android:id="@+id/productName"
        android:layout_width="200dp"
        android:textColor="@color/btnColor"
        android:layout_height="match_parent"
        android:textAppearance="@style/TextAppearance.AppCompat.Caption"/>

    <Button
        android:id="@+id/productAmount"
        android:layout_width="15dp"
        android:layout_height="25sp"
        android:layout_marginRight="5dp"
        android:textAlignment="center"
        android:textColor="@color/btnColor"
        android:background="@android:color/transparent" />
    <!-- android:inputType="number" -->
    </LinearLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="right">
    <ImageButton
        android:id="@+id/delImgButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/close_circle_s"
        android:tint="@color/btnColor"
        android:layout_gravity="right"
        android:background="@android:color/transparent"
        android:layout_marginRight="5dp"
        />
    </LinearLayout>
    <!--
    <Button
        android:id="@+id/delButton"
        android:layout_width="60dp"
        android:layout_height="match_parent"
        android:layout_marginRight="5dp"
        android:layout_marginLeft="5dp"
        android:text="del"/>
-->
</LinearLayout>